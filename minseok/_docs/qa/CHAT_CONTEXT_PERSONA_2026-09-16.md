# 5차 페르소나 테스트 — 멀티턴 맥락 단절 (2026-09-16)

> 배경: 사용자가 "최근 채팅에서 맥락이 이어지지 않는다"고 보고했으나 턴을 특정하지 못해
> 맥락 승계 가설 10개(H1~H10)로 시나리오를 짜 실측했다. 배포본 `d0f895d`(Gemma 4 e4b QAT),
> 계정 `+claudetest5`(정리 대상), `/chat/ask` 10시나리오 52턴, 에러 0, 응답 중앙값 12.8s.
> 러너·시나리오·결과 원본: `~/.claude/projects/-home-host-projects-com-redoceanmap/persona5/`
> (`results-20260916.jsonl`). 대화 id 256~265.

## 구조적 사실 (코드 확인)

히스토리는 **phase0 의도 분류**와 **phase1 상권 후보 선택**에만 들어간다(`_history_block` —
최근 6턴 × 200자). 최종 답변을 쓰는 네 경로(stock 서술 · market_news 서술 · general ·
market phase2)는 현재 질문 + 데이터 컨텍스트만 받는다. `llm_orchestrator.orchestrate()`는
이미 `history=` 인자를 지원하므로 서술 경로에 넣는 데 새 인프라는 필요 없다.

## 원인별 결함과 수정안

| # | 원인 | 실측 (시나리오 t턴) | 수정안 |
|---|---|---|---|
| A | **서술 단계 히스토리 부재** — 매 턴 처음부터 재서술. "리스크는?"에 개요, "처음 답과 다른데?"에 대조 없음, "결론이 뭐였지?"에 새 분석 | S1 t2·t4·t5, S2 t3, S3 t5, S4 매 턴 결론 반복, S5 전부, S10 t2·t4 | ① stock(2196행)·market_news(2028행)·general·phase2 서술 호출에 `history=` 전달 — 최근 assistant 2~3턴, 턴당 400자 상한(≈600토큰, phase1 5,000토큰 프롬프트에는 넣지 않음). ② 회상 질문("결론/정리/다시 말해/처음에/뭐였지")은 LLM 재분석 대신 직전 assistant 텍스트를 코드로 재제시(`_NEWS_DETAIL_RE` 선례) |
| B | **뉴스 검색 질의가 직전 주제를 안 실음** — 후속 문장 그대로 검색해 무관 기사 근거 | S2 t4(반도체→오라클·오픈AI), S6 t5(테슬라→SK하이닉스), S7 t5(현대차→일반 뉴스) | ① market_news 경로 검색 질의 = 직전 종목 카드 심볼(`payload.stock.symbol`) 또는 직전 market_news 질문 + 현재 질문, 심볼 있으면 `ticker=` 승계. ② `_inherit_intent`를 market_news 판정에도 적용(현재 general만). ③ `_NEWS_DETAIL_RE`에 "그 종목 뉴스" 허용(`(?:그|아까|방금)\s*(?:종목\s*)?(?:뉴스|기사)`) |
| C | **정정 토큰 오탐이 업종 승계 해제** — "라고 했"·"잘못"이 `_has_correction`에 걸려 phase1이 업종을 새로 고름 | S8 t4(카페→중식), S9 t2(헬스장→운동/경기용품, 이후 4턴 고착) | 정정 토큰이 업종 승계를 풀 때는 `_service_hinted` 또는 업종 어휘 동반 시로 한정. 지역 정정("잘못 말했어, 강동구야")은 지역만 교체 |
| D | **결정론 가드·의도 분류가 회상·비교 질문을 가로챔** | S4 t7("처음 업종 뭐였지?"→`_SERVICE_FOCUS_RE` 업종 랭킹), S10 t5("결론만 다시 말해줘"→general→서비스 소개), S3 t4·S9 t5(상권↔주식, 송파↔강동 교차 비교 미지원) | 회상 정규식(`뭐였지|기억해|처음에|다시 말해`)을 결정론 가드보다 앞에 두고 A-②로 처리. `_DOMAIN_FOLLOWUP_RE`에 "결론·정리" 추가. 교차 비교는 미지원 고지(발명 금지) |
| E | **지시어 가드가 후보를 1개로 축소 → 서수 지시 불가** | S4 t8("두 번째"), S5 t2~t4("마지막"·"순서대로" 전부 1위 답), S8 t3("다른 구"에도 신촌) | ① 서수("두 번째/마지막/N위")는 직전 **복수** 추천 payload 인덱스로 코드 해석. ② `_previous_area_codes`가 마지막 payload가 아니라 마지막 복수 추천 payload를 본다. ③ `EXCLUSION_TOKENS`에 "다른 구/다른 지역" |
| F | **사용자 선언 정보(예산) 미보존** — 프롬프트에만 파싱 | S8 t2·t5("예산을 말씀해 주시면") | `_budget_notice`가 현재 프롬프트에 없으면 history의 user 메시지를 역순으로 `parse_budget_krw` |
| G | **같은 후보에 1순위가 턴마다 바뀜** — phase2 순위 비결정 | S3 t1 성수동카페거리→t4 서울숲역→t5 복귀, S5 t1 신논현역→t2 도산공원북측 | 후보 집합이 같고 지시어 후속이면 직전 순위 유지(코드), 또는 `_market_verdict_line` 1순위를 결정론 정렬로 고정 |

권장 착수 순서: **리졸버(별건 1) → B → C → E → A → D → F → G**. B·C·E는 결정론 코드
수정이라 골든셋 게이트로 바로 검증되고, A는 프롬프트 토큰 예산(`prompt_eval_count` 경고)을
확인하며 넣는다.

## 별건 (맥락과 무관하지만 심각)

1. **종목 리졸버 "현대차" → 현대차증권(001500, 7,900원)** — S7 전 턴. KRX 상장명이
   "현대자동차"라 부분 일치가 "현대차증권" 하나에만 걸린다(파드 안 `_resolve_sync("현대차")`
   실측). `_KR_FALLBACK_CODES`의 "현대차": "005380"은 KRX 실패 시에만 쓰인다.
   수정: `_KR_NAME_ALIASES`에 "현대차": "현대자동차" 추가, 부분 일치 다건 시 접두 일치 우선.
   골든셋 SK05는 `accepted_queries`만 보고 심볼을 검증하지 않아 못 잡았다 — 심볼 검증 추가.
2. **phase0 비교 질문에서 종목 1개만 추출** — S10 t1 "삼성전자랑 SK하이닉스 중"에
   `stock_query: "삼성전자"`(Ollama 직접 재현 동일). 비교 경로가 안 탄다. SF06류 골든셋 확인.
3. 예산 고지 문구 두 갈래 동시 출력 — S8 t1 "빠듯해요(넘어요)".

## 통과한 것

종목·지역·업종 단순 승계(S1 5턴·S4 8턴·S7 t2), 지역 정정(S9 t2), 잡담 뒤 도메인 복귀
(S6 t3), 뉴스 상세 결정론(S2 t2), 알림 안내 결정론(S7 t3), 예산 고지 첫 턴(S8 t1),
등급 '주의' 고지 지속(S5·S9).

---

## 수정 결과 (2026-09-16 당일, window 세션)

착수 순서 리졸버→B→C→E→A→D→F→G 전부 구현. 검증은 ① 단위 테스트(chat 15건·리졸버 1건 추가, 전체
1,022+ 통과) ② import-linter 5계약 ③ 골든셋 134문항 러너·게이트 ④ 같은 러너·시나리오로
**수정본을 띄운 로컬 백엔드(8010, 코드 마운트)**에 52턴 재실행(`results-after-20260916.jsonl`).

| 원인 | 구현 | 재실행 확인 |
|---|---|---|
| 리졸버 | `_KR_NAME_ALIASES["현대차"] = "현대자동차"` | S7 현대차 364,000원(005380) |
| B | `_news_query`(지시 표현이면 직전 비지시 질문을 검색 질의 앞에 붙임·직전 종목 카드면 ticker) · phase0 종목 미추출/지시어 market_news → 직전 종목 카드 승계 · `_NEWS_DETAIL_RE` "그 종목 뉴스" | S6 t5 테슬라 헤드라인 나열, S7 t5 종목 승계 |
| C | 정정 토큰이 업종 승계를 푸는 조건을 "업종" 동반 시로 한정 | S9 스포츠클럽 유지, S8 t4 업종 표류 없음 |
| E | `_ordinal_index`+`_previous_multi_codes`(서수→직전 복수 추천 인덱스) · `_OTHER_DISTRICT_RE`("다른 구/지역"→직전 자치구 제외) | S4 t8 서교동(2위), S5 t4 마지막 상권, S8 t3 마포→동대문 |
| A | `_narration_history`(최근 2쌍, 답변 400자)를 stock·market_news·phase2 `orchestrate(history=)`·general 프리앰블에 전달 + 히스토리가 있을 때만 `HISTORY_NOTE`를 컨텍스트 끝에 부착 · `_recall_text`(회상 질문은 LLM 없이 직전/처음 답변 재제시, 복수 추천은 순서 병기, 사용자 발화 회상은 인용) | S1 t2 리스크(PER)·t3 매물대 위치 즉답, S4 t2~t6 물은 항목 첫 문장, S1 t5·S10 t5 결론 재제시 0초 |
| D | 회상 게이트를 결정론 가드·phase0 앞에 배치 · `_DOMAIN_FOLLOWUP_RE`에 결론·정리 · `_CROSS_COMPARE_RE` 상권↔종목 비교 미지원 고지 | S4 t7 처음 질문 인용, S3 t4 고지, S3 t5 처음 추천 재제시 |
| F | `_budget_notice(history)` — 프롬프트에 예산이 없으면 앞 턴 user 발화에서 파싱 · "빠듯해요(넘어요)"→"넘어요" · 업종 미언급 예산 후속 문두 보강 | S8 t2·t5 예산 5,000만원 반영, t4 예산 회상 |
| G | 지역 미언급 후속은 직전 추천 순서로 정렬해 이어받고 phase2에 "[직전 답변의 1순위]" 힌트 | S3 t3 성수동카페거리 유지 |

재실행 52턴 에러 0 · 결정론 즉답 3→12턴. 응답 중앙값 12.8→17.8s는 골든셋 러너와 Ollama를
동시에 써서 순수 증가분이 분리되지 않았다(히스토리 추가분은 ≈600토큰).

**골든셋 게이트**: 1차 환각 1건(SU11 — 단일턴에서 모델이 "195.30" 안에 마커를 끼워 "[1]95.30",
가드 무관·확률 변동) · 2차 환각 0건이나 인용 커버리지 0.976→0.943(허용 −3%) — 서술 프롬프트
상수에 상시 추가한 "이전 대화" 규칙이 단일턴 인용 준수를 희석. 규칙을 상수에서 빼고 히스토리가
넘어갈 때만 `HISTORY_NOTE`로 붙이도록 바꿔 단일턴 프롬프트를 baseline과 동일하게 되돌림 → 3차 **게이트 통과**(의도·종목·지역·승계 1.0, 인용 커버리지 **1.0**, 환각 0·잘림 0·절대 위반 0). trace.jsonl은 3차 산출물.

### 잔여 소진 (같은 날 저녁, "남은거 진행하자")

| 항목 | 구현 | 확인 |
|---|---|---|
| 별건 2 — 비교 질문 종목 1개 추출 | phase0가 종목 하나만 주고 우열 표현(`_COMPARE_ASK_RE` — 비교·vs·"중"·"뭐가 더/나아")이 있으면 `_split_compare_queries`가 연결어(랑·이랑·와·과·하고·,·vs) 앞뒤 어절을 보탠다. stopword(주가·배당·확률·뉴스…)로 거르고, 연결어만으로는 분해하지 않는다("배당이랑 오를 확률"이 비교로 잡혔던 단위 테스트 실측) | S10 t1 비교표(삼성전자·SK하이닉스 나란히), t2 '더 낫다고 한 쪽' 리스크 |
| 지역↔지역 비교 | 언급된 자치구가 2곳 이상 + 비교 어휘면 자치구마다 대표 상권 하나씩(phase1 선택 우선, 없으면 매출 상위)을 후보로 두고 phase2에 `[지역 비교]` 줄을 준다 | S9 t5 천호역(강동)+가락시장(송파) 두 후보, 결론 줄에 지역 명시 |
| 특정 턴을 겨눈 회상 | "리스크/위험/우려" 회상은 그 어휘를 쓴 답변이 없으면 재제시하지 않고 정상 흐름으로 보낸다(서술 경로가 히스토리를 받아 앞 대화 근거로 다시 설명) | S2 t4 1턴 업황 답변('우려' 포함) 재제시 |
| 사용자 발화 회상 뒤 재추천 | 사용자 회상 + 재요청 동사(`_REPICK_RE` 골라·추천·찾아·분석·비교)면 원 질문을 인용한 뒤 **원 질문으로** 도메인 라우팅(`_route_domain` — ask()에서 분리)을 다시 돈다. 업종 랭킹 가드("업종")를 원 질문이 타지 않는다 | S4 t7 처음 질문 인용 + 분식 기준 마포구 재추천 |
| 테스트 계정 정리 | users 21·22·23·24·34(claudetest·2·3·4·5)와 대화 137건·메시지 2,202건·역할 5건을 한 트랜잭션으로 삭제. 삭제 전 CSV 백업은 세션 스크래치패드 `account-cleanup/`(세션 종료 시 소멸 — 04:00 일일 백업이 정본). 재확인용 임시 계정 claudetest6도 검증 뒤 같은 방식으로 삭제 | DB users 0건 |

검증: 단위 테스트 1,027건(chat 4건 추가)·import-linter 통과 · 골든셋 4차 **게이트 통과**(의도·종목·지역·승계·인용 1.0, 환각 0·잘림 0·절대 위반 0). trace.jsonl은 4차 산출물

### 남은 것 소진 (같은 날 밤, "남은것 변경 시작하자")

| 항목 | 구현 | 확인 |
|---|---|---|
| 턴 지목 회상 | `_TURN_RECALL_RE`("두 번째 답변"·"첫 질문"·"마지막 답변") → `_recall_turn`이 (답변/질문, 인덱스)를 돌려주고 `_recall_text`가 그 답변(질문이면 바로 뒤 답변)을 재제시. "내가 N번째 질문에서 뭐라고 했지?"는 그 질문을 인용 | L1 t3 '첫 번째 답변 다시 보여줘' → 1턴 답변 재제시, t4 '내가 두 번째 질문에서 뭐라고 했지?' → 2턴 질문 인용(둘 다 0초) |
| 동·상권명 단위 비교 | `_compare_place_groups` — 연결어(랑·이랑·와·과·하고·,·vs) 앞뒤 어절마다 지명 언급 판정을 돌려 구절별 상권 집합을 만들고(2개 미만이면 자치구 묶음 폴백), 구절마다 대표 상권 하나씩 후보에 둔다. 자치구 비교와 같은 코드 경로 | L2 '성수동이랑 연남동 중' → 연남동(홍대)·성수동카페거리, '잠실역 vs 석촌고분역' → 잠실역·석촌고분역. 첫 실기동에서 결론 줄(성수동)과 본문(연남동)이 모순 → 지역 비교는 축 어휘가 없어도 `_superlative_pick(default_axis="sales")`로 점포당 월매출 우열을 코드가 정하게 보강, 재확인 일치 |

검증: 단위 테스트 1,030건(chat 3건 추가)·import-linter 통과 · 골든셋 5차 **게이트 통과**(의도·종목·지역·승계 1.0, 인용 0.988, 환각 0·잘림 0·절대 위반 0 — 동시 부하로 33분 소요). 5차 trace는 `default_axis` 보강 직전 코드로 생성됐으나 골든셋에 지역 비교 문항이 없어 게이트와 무관 ·
임시 계정 claudetest7로 실기동(L1·L2) 뒤 삭제.

### 남은 것

- 없음(이 문서 범위). 지역 비교는 구절이 지명으로 해석될 때만 성립한다 — "여기랑 저기"처럼
  지시어끼리는 비교하지 않는다(직전 추천 승계 경로).
- 향후 페르소나 러너를 다시 돌리려면 테스트 계정을 재가입해야 한다(`POST /auth/register`, 5회/시간).
