# 서비스 품질 개선 — 프로덕션 페르소나 테스트 후속 실행 계획

> 2026-08-31 수립. 백엔드 PC(window 브랜치)에서 이어서 작업하기 위한 정본이다.
> [[SIGNAL_OVERHAUL_2026-08|시그널 대개편]]·[[ROADMAP]]의 기존 계획에 **프로덕션 실측 테스트
> 2회(2026-08-31, 시나리오 30건)** 발견을 병합해 착수 순서를 다시 세웠다.
> "시작하자"라고 하면 Phase 0부터 순서대로 진행한다.
>
> 테스트 방법: 실서비스(redoceanmap.com)에 테스트 계정으로 회원가입→로그인→`/chat/ask`
> 30건(상권 15·주식 15, 초보/일반인/전문가 페르소나) + 페이지 API(`/stock/{s}/forecast`·
> `/market/trdar/{c}/score`·`/stock/board`) 대조. 증거 인용은 본문에 박제했다(스크래치 원본은
> 맥 세션 한정이라 이 문서가 유일한 정본).

## 테스트 계정 (정리 대상)

- `jang971121+claudetest@gmail.com` (name 클로드테스트) — 대화 id 109~136이 프로덕션 DB에 남아 있다.
- `jang971121+claudetest2@gmail.com` (name 클로드테스트2, 2026-09-01 신규) — 1차 계정 비밀번호가
  맥 세션 한정이라 백엔드 PC 스팟 체크용으로 재가입. 비밀번호는 세션 스크래치패드 한정.
- 처리: 유지(재테스트용) 또는 삭제는 사용자 결정. 골든셋 편입 후 삭제 권장.

## 전역 원칙 (분석 개선 사이클 — 2026-08-04 사용자 승인, 메모리 정본)

1. **측정이 변경을 감싼다** — 프롬프트·컨텍스트 주입을 바꾸기 전에 골든셋 baseline을 먼저 박제한다.
   baseline 없이 프롬프트 변경 커밋 금지.
2. **재료가 구조보다 먼저** — 데이터가 없는데 서술 규칙만 주면 환각을 유발한다.
3. **결정론 우선** — 소형 LLM(7.8B)에 맡기지 않고 코드가 문장을 붙인다(I-12~I-18 공통 처방과 동일).
4. 검증은 전부 도커 경유(호스트에 pytest 없음):

```bash
docker run --rm -v /home/host/projects/com.redoceanmap:/work -w /work \
  -e PYTHONPATH=/work/minseok:/work/minseok/apps \
  minseok97/redoceanmap-backend:latest python -m pytest minseok/apps -q -p no:cacheprovider
docker run --rm -v /home/host/projects/com.redoceanmap:/work -w /work/minseok \
  -e PYTHONPATH=apps minseok97/redoceanmap-backend:latest lint-imports --config .importlinter
```

---

## Phase 0 — 배포 + baseline 박제 (다른 모든 것의 전제)

### 0-0. 맥 작업분(2026-08-31) 선점검 — 배포 전에 이것부터

맥에서 Phase 1의 4항목 + Phase 3 일부를 구현·커밋했다(`6d0fce0` 가드 4종+어닝 배지 ·
`be7e28d` 2단계 노출·한계 고지·골든셋 129). 맥 검증에는 **환경 한계가 있었으므로**
백엔드 PC에서 아래를 먼저 재확인하고 시작한다:

1. **pull 확인** — `git log --oneline -1`이 `be7e28d` 이상인지. 세 브랜치는 동기 상태로
   push돼 있다(2026-08-31).
2. **전체 pytest 전건 통과 확인** — 맥에서는 auth 7건이 실패했는데 원인은 Docker Hub의
   낡은 공개 이미지(2026-07-08·py3.11, redis 모듈 부재 + `JWSSignatureError`)였고 **변경 전
   HEAD에서도 동일 재현**됐다. 백엔드 PC 로컬 이미지(최신)에서 auth 포함 전건 통과가
   진짜 판정이다. lint-imports 5계약도 함께.
3. **(제안) `docker push minseok97/redoceanmap-backend:latest`를 배포 절차에 추가** —
   Hub의 latest가 7월 8일자라 맥에서 전체 스위트를 신뢰할 수 없었다. 배치 cron 2종이
   이 태그를 쓰므로 push 자체는 부작용 없음.
4. **배포 직후 프로덕션 스팟 체크 6문** (테스트 계정 `jang971121+claudetest@gmail.com`,
   1차 테스트와 같은 방식의 `/chat/ask` 호출):
   | 질문 | 기대 결과 (맥 수정분) |
   |---|---|
   | "테슬라랑 애플 중 어디에 투자하는 게 나아?" | 오류 대신 테슬라 단독 분석 + 비교 미지원 고지 문두 |
   | "유동인구 많고 폐업률 낮은 상권 3곳 추천해줘" | 422 대신 200 안내(질문 방법 + 랭킹) |
   | "너희 UP 시그널의 백테스트 적중률이 어떻게 돼?" | 재적합 실측 수치(표본·Wilson 하한·게이트) 결정론 답변 |
   | "SK하이닉스 매물대와 지지 저항 구간 분석해줘" | 지지선 원화 **정수** 표기(소수점 없음) |
   | "홍대 근처에서 카페를 창업하려고 하는데 상권 분석해줘" | 카드에 "None개월" 없음 · "이 업종 매출 데이터 없음" 문구 |
   | 프론트 /market 상권 선택 | 요약 타일 3종 + "전체 지표 더 보기" 접힘 + 말미 한계 고지 |

### 0-0 실행 결과 (백엔드 PC 2026-09-01)

- 선점검 ①~② 통과: pytest 전건(auth 포함 — 맥 실패는 낡은 Hub 이미지 탓 확정)·5계약 KEPT.
- ③(docker push 제안)은 미실행 — 사용자 결정 대기.
- ④ 스팟 체크: 5/6 통과(⑥ 프론트 /market은 사용자 수동 잔여). 발견 1건 즉시 수정:
  phase0 종목 질의의 **세 번째 변형**(쉼표 결합 단일 문자열 "테슬라, 애플" — 리스트/리스트
  문자열에 이어)이 리졸버를 통째로 죽임 → `_normalize_stock_queries`에 쉼표·가운뎃점 분해
  추가 후 재배포·재검증 통과. p04(등급 고지+추이 인용)·p06(목동 200, 실상권 2곳) 재검증 통과.
- ~~관찰(후속 확인): q05 "기준선 0%"~~ — ✅ 원인 규명·수정(2026-09-01): 리포트 행 payload에
  baseline 키가 없어 게이트웨이 폴백 0.0이 소비됐다(기준선은 보드 레벨 baseline_up_rate).
  ForecastRefitGateway가 보드 기준선을 행에 채우도록 수정 + 회귀 테스트.

### 0-1. 미배포 커밋 배포

프로덕션 `/health`가 `03832d6`(빌드 2026-08-28)을 보고했다. 이후 **9커밋**이 미배포다 —
`3d8ecbb` 적중 재정의 · `3ab4e13` num_ctx 8192 · `ac081a5` **answer_guard+판정 배지** ·
`cfb1758` LLM 감시 · `6f02dab`~`5f43b49` docs 3건 · `6d0fce0` **가드 4종+어닝 배지** ·
`be7e28d` **2단계 노출·한계 고지**.

- ✅ **배포 완료(2026-09-01)**: deploy.sh 2회(스팟 체크 발견 수정 반영 재배포 포함),
  `/health` = `9229147`(빌드 02:22Z). ⚠ 작업 트리 빌드라 **9/1 미커밋 수정분
  (Phase 1~3 + 쉼표 변형 방어)이 함께 배포됨** — 커밋 후 스탬프 재정합 필요.
- 작업: 백엔드 PC pull → deploy.sh (push는 2026-08-31 완료 — 3브랜치 `be7e28d` 동기).
- **검증**: `/health` version.commit이 `be7e28d`. 0-0의 스팟 체크 6문 통과. 그 다음
  테스트에서 발견한 **카드-본문 모순 5건**(P3, 아래)을 재실행해 answer_guard가 실제로
  잡는지 확인한다 — 잡히면 P3 종결, 안 잡히면 Phase 2에서 가드 확장.

### 0-2. 골든셋 baseline + 회귀 케이스 편입 — ✅ baseline 박제 완료(2026-09-01)

> 실행 결과: 129/129 성공(오류 0)·17분 27초·OOM 없음(이날은 가중치 GPU 상주 —
> llama-server 2.5GB로 8/28의 7.7GiB 조건과 달랐다. `.wslconfig` 상향 없이 완주).
> 새 baseline: intent 1.0 · stock_query 1.0 · region_hit 1.0 · volume_verdict 1.0 ·
> risk_mention 1.0 · citation_coverage 0.6(**R4 baseline 겸** — "남은 것" 1번 종결) ·
> 환각 5 · 잘림 0 · 절대 규칙 위반 0(신설 grade_caution 포함). 구 baseline(120문항)은
> 골든셋이 129로 바뀌어 재박제(구본은 git 이력).
> **2026-09-03 재박제(134문항)** — 4차 마감 절 참고(citation 0.911·환각 1).
> 발견: MR21 답변이 "평균 대비 성장도: 200%/22%"라는 **없는 지표명을 발명** — I-15
> 상권판 확장 후보(지표명 화이트리스트)로 기록. 잔여 편입 후보(p04 왜곡 등)는 아래 유지.

- 골든셋 러너(`-m ollama`)를 실행해 baseline을 박제한다(아직 미측정 상태).
  **5건은 이미 편입됨(맥 2026-08-31, 124→129)**: s3 비교(SF06)·s4 확률(SU11)·m5 임대료
  (MR25)·p06 목동(MR26)·m4 조건검색(MN11). q05 메타 질문은 러너의 intent 기록 의미론이
  불확실해 골든셋 대신 **단위 테스트 3종**으로 고정했다.
- **잔여 편입 후보**(baseline 후 채점 축과 함께 추가): p04 성수역 왜곡, q09 재라벨,
  m1 49개월 재라벨, q08 급등주, q06 초보 용어, q01 모순 — 이들은 기존 채점기로 못 재는
  유형이라 Phase 2의 준수율 지표 신설과 같이 넣어야 의미가 있다.
- ⚠ 실행 전 호스트 메모리 확인 — 2026-08-28 골든셋 실행이 커널 OOM으로 중단된 이력
  (llama-server anon-rss 7.7GiB / WSL2 10.7GiB). `.wslconfig` 상향 또는 비필수 컨테이너 정지 선행.

---

## Phase 1 — 신뢰 파괴 버그 (테스트 발견 · 전부 신규)

우선순위 순. 각 항목은 실측 증거를 그대로 인용한다.

### 1-1. ~~점수 의미론 왜곡 — "주의" 등급 상권을 사실 반전으로 강력 추천~~ ★최우선 — ✅ 구현 완료(백엔드 PC 2026-09-01, 미커밋)

> 구현: `_score_text`가 컴포넌트·총점마다 "서울 평균 상회/미달" 방향을 결정론 문장으로
> 병기. `answer_guard.suppress_recommendation`(추천→검토·강조 부사 제거) +
> `grade_caution_notice`(등급 고지 문두 삽입)를 '주의'/'위험' 상권에 적용.
> `eval_scorer`에 `grade_caution` 절대 규칙 신설(구 트레이스 발화 0건 확인).
> 잔여: 배포 후 p04 재실행 검증.

- **증상(p04 실측)**: 답변 "성수역을 강력히 추천합니다. 유동인구 성장률이 서울 평균을 크게
  상회 (47.9점)". 원데이터(`/market/trdar/{성수역}/score`): 유동인구 성장 value **-0.11%**
  (벤치마크 +0.72% **미달**), 컴포넌트 점수 47.9(50=서울 평균 미달), **총점 44.9 '주의' 등급**.
- **원인**: LLM이 "점수 50=서울 평균" 의미론을 모른 채 숫자를 자유 서술. phase2 컨텍스트에
  점수의 해석(등급·평균 대비 방향)이 결정론 문장으로 없다.
- **수정**:
  - `chat_interactor`의 상권 컨텍스트에 컴포넌트별 해석을 **코드가 문장으로** 넣는다:
    "유동인구 성장 47.9점(서울 평균 50 **미달**, 실측 -0.11% vs 서울 +0.72%)" 형식.
  - **등급 결정론 가드**: 총점 등급이 '주의'/'위험'인 상권에는 "추천"·"강력히" 어휘를
    후처리로 차단하고, 등급을 답변 첫 문단에 코드로 삽입한다(eval_scorer에 금지 패턴 추가).
- **검증**: p04 재실행 — "주의 등급" 명시 + 추천 어휘 부재. 채점기 회귀 테스트.

### 1-2. ~~자기 서비스 메타 질문 미인지 (q05)~~ — ✅ 구현 완료(맥 2026-08-31, `6d0fce0`)

- **증상(실측)**: "너희 UP 시그널의 백테스트 적중률·표본·신뢰구간은?" →
  "'UP 시그널'은 마케팅 용어인 경우가 많습니다… **해당 서비스 측에 데이터를 요구하세요**…
  어떤 서비스를 염두에 두고 계신가요?" — 1,784자 모델 상식(MDD·슬리피지·과적합).
  자기 데이터(표본 3,301·Wilson 구간·재적합 리더보드)를 보유하고도 제3자 취급.
- **원인**: phase0 의도분류에 서비스 메타 질문 분기가 없어 무근거 일반 생성으로 낙하.
- **수정**: phase0에 `service_meta` 분기 추가(키워드: 너희/이 서비스/시그널 적중률/백테스트 등)
  → **결정론 응답**: 활성 판정 조합·검증 방식(변동성 초과 적중·Wilson 하한·기준선)·현재 표본을
  `forecast_refit_reports`/`SignalConfigPort`에서 읽어 템플릿으로 답한다. LLM 미사용.
  이 서비스의 최대 자산(측정해서 기각하는 정직성)을 답변으로 노출하는 것 자체가 차별화다.
- **검증**: q05 재실행 — 자기 수치로 답변. 인터랙터 테스트(스텁 포트) 1종.

### 1-3. ~~비교 질문: 종목 리스트 repr가 리졸버로 전달 (s3 · I-18 선행)~~ — ✅ 구현 완료(맥 2026-08-31, `6d0fce0`)

- **증상(실측)**: "테슬라랑 애플 중 어디에 투자?" → `"종목을 찾지 못했습니다: ['테슬라', '애플']"`
  — 파이썬 리스트 표기가 사용자에게 그대로 노출. `_OVERSEAS_ALIASES`에 두 별칭 모두 있는데도
  실패(`symbol_resolver.py:114`에 리스트 문자열째 도달).
- **원인**: phase0가 뽑은 복수 종목을 chat이 단일 질의 문자열로 넘긴다(정확한 지점은
  chat_interactor의 stock 질의 조립부 — 착수 시 특정).
- **수정(I-18 계획 그대로 + 방어)**: phase0 스키마에서 복수 종목이면 **첫 종목만 분석 +
  "비교는 아직 지원하지 않아 {첫 종목}만 분석했다"를 결정론 문장으로 문두 삽입**.
  리졸버 앞단에 리스트형 입력 방어(첫 요소 추출)도 넣는다.
- **검증**: s3 재실행 — 테슬라 단독 분석 + 고지 문장. 회귀 테스트 1종.

### 1-4. ~~상권 매칭 실패 = 422 원문 노출 + '목동' 커버리지 (m4·p01·p06)~~ — ✅ 완료(안내 응답 `6d0fce0` + 랭킹 라우팅·목동 백엔드 PC 2026-09-01, 미커밋)

> 목동 원인(실DB 확인): 목동 상권은 실재(목동문화체육센터·목동로데오거리 등, 목1~2동).
> `_place_stem`이 선행 한글만 취해 "목1동" 어간이 "목" 1자로 붕괴 → 2자 미만 탈락.
> 퇴화 시 숫자 제거 후 재시도로 수정(기존 어간 무손상, eval_scorer._stem 동일 반영).
> 랭킹 라우팅: 허브 `CommercialDataPort.get_area_ranking`(+`AreaRankingInfo`) 신설,
> market 게이트웨이가 랭킹 슬라이스 재사용(실DB 1,650행 검증). 지역 미언급+조건 어휘
> (폐업률 낮은/유동인구 많은/매출 높은)면 phase1 없이 코드가 정렬해 N곳 응답.
> ⚠ 계획의 "랭킹에 유동인구 필터 보유"는 오인 — 랭킹 집계에 유동인구 축이 **없다**.
> 유동인구 요청은 정렬한 척하지 않고 미지원 고지(I-12 태도). 축 추가는 별도 판단.

- **증상(실측 3건)**: ① "유동인구 많고 폐업률 낮은 상권 3곳"(지역명 없음) → 62초 뒤
  `{"detail":"유효한 상권을 찾지 못했습니다."}` ② "서울에서 어디가 안전한지"(광역 질문) → 동일 422
  ③ **"목동에서 반찬가게"** — 실재 지역인데 422(86초). 양천구 목동 상권 매칭 실패는 원인 조사 필요
  (`_mentioned_codes` 어간 매칭이 '목동'을 못 잡는지, 상권명에 '목동'이 없는지 실데이터 확인).
- **수정**:
  - **422를 사용자에게 그대로 내지 않는다**: `NoValidAreaError`를 200 안내 응답으로 변환 —
    "지역 이름(동/역/거리)을 함께 물어봐 주세요. 예: 강남역 카페" + 조건 질의면 랭킹 안내.
  - **조건 질의 라우팅(I-14 확장)**: "폐업률 낮은/유동인구 많은 N곳" 패턴을 감지하면 LLM 대신
    기존 `/market/areas/ranking`(폐업률·유동인구 필터 보유)을 호출해 결정론으로 N곳을 채운다.
  - 목동 원인에 따라 별칭 보강(행정동 축은 I-3으로 이미 있음 — `adm_dong` 매칭 경로 연결 검토).
- **검증**: m4·p01·p06 재실행 — 3건 모두 200 + 유용한 응답. 라우터 테스트.

### 1-5. ~~서술 텍스트 품질 버그 4종 (소형·일괄)~~ — ✅ 구현 완료(맥 2026-08-31, `6d0fce0`)

1. **`None` 노출**: `operatingMonthsText` = "…(지역 평균 **None**개월)" — narrator에서 결측 시
   해당 구를 생략. (m1 실측)
2. **숫자 포매팅**: "지지선인 **1,245,729.86원**"(q10) — 원화는 정수 반올림, 달러는 소수 2자리
   상한. 컨텍스트 조립부에 포맷 함수 일원화.
3. **forecast 인사이트 주어 누락**: position insight "…60일 저점보다 +35.8% 위에 있습니다.
   RSI 49(중립 구간). **하루 평균 변동폭(6.4%)의 5배입니다.**" — 무엇의 5배인지 없음.
   `forecast_narrator`의 position 문장을 "지지선까지 여력은 하루 평균 변동폭의 5배입니다"로 명시.
4. **카드-페이지 모순 표기**: 채팅 카드 `monthlyRevenueText`="매출 데이터 없음"인데 같은 상권
   점수 페이지는 월매출 366억 표시(m1 실측 — 카드는 업종별, 페이지는 상권 전체).
   카드 문구를 "**카페 업종** 매출 데이터 없음(상권 전체 매출은 상세에서)"로 축을 명시.
- **검증**: narrator·컨텍스트 단위 테스트 + m1·q10 재실행.

### 1-6. ~~재라벨 왜곡 2건 → I-15 가드의 실증 입력~~ — ✅ 가드 구현 완료(백엔드 PC 2026-09-01, 미커밋)

> 상권판 `strip_unsupported_metric`: 폐업률 '데이터 없음' 상권의 이유에서 "폐업률" 문장을
> 제거(제거된 유의 문장은 `_ensure_risk_note`가 데이터 기반으로 재충전). 주식판
> `enforce_distance_claim`: 매물대 거리>ATR×2면 '근처/부근/인근'을 "(현재가보다 N% 아래/위)"로
> 교체(매물대·밀집 문장만, 지지선 등 타 지표 불간섭). 잔여: 골든셋 회귀 케이스 편입(0-2 뒤).

- **증상(실측)**: ① m1 "높은 폐업률(평균 49개월 내 폐업)" — 원데이터는 `closureRateText: 데이터
  없음`이고 49개월은 **폐업 점포의 평균 영업 기간**(별개 지표). 폐업률 데이터가 없는데 "높은
  폐업률" 단정. ② q09 "과거 거래량 밀집 구간(180.10~186.34달러) **근처**" — 현재가 227.97로
  **23% 위**. "근처" 아님.
- **수정**: I-15(값 재라벨 금지 가드)를 주식+상권 양쪽으로 설계. 상권판: 컨텍스트에 없는 지표명
  ("폐업률")이 수치와 함께 등장하면 차단·재생성. 주식판: 거리 서술("근처")을 코드가 임계
  (예: ATR 2배 이내)로 판정해 위반 시 교체.
- **검증**: 골든셋 회귀 케이스 2건.

---

## Phase 2 — 결정론 가드 일괄 (기존 I-11~I-18 + 테스트 신규 I-19~I-21)

로드맵의 기존 권고 1순위. 테스트가 프로덕션 재현을 확인했으므로 계획 그대로 진행하되,
신규 3건을 같은 방식(코드가 문장을 붙인다)으로 추가한다. 정본 표는 [[ROADMAP]] §제약·근거
프로브 — 여기는 착수 순서와 테스트 크로스레퍼런스만 둔다.

| # | 항목 | 테스트 재현 | 비고 |
|---|---|---|---|
| I-11 | phase1 후보 표에 판정 축(폐업률·점포당 매출·YoY·연령·성별) | m2(무근거 커피 추천 — "매출 데이터 없어 확인 필요"라면서 추천), p04 | ⏳ **보류** — phase1 프롬프트가 창(NUM_CTX 8192)에 근접, 열 추가는 prompt_eval_count 실측(Ollama) 선행 |
| I-12 | ~~미지원 축 명시~~ | m5, q03 | ✅ 2026-09-01 — `_unsupported_notice` 문두 결정론 삽입(market: 임대료·정확 매출 / stock·market_news: 배당·PER비교) |
| I-13 | 시점 불일치 고지 | (이번 미재현 — 프로브 M5 근거) | 미착수(재현 근거 없음 — 재현되면 착수) |
| I-14 | ~~개수·정렬 제약 결정론 집행~~ | m4 | ✅ 1-4의 랭킹 라우팅으로 종결 |
| I-15 | ~~값 재라벨 금지 가드~~ | m1·q09 | ✅ 1-6에서 구현(골든셋 회귀 편입만 잔여) |
| I-16 | 하락 단정 가드 | s2("하락세 지속 가능성" vs 카드 NEUTRAL) | **0-1 배포 후 재측정** — answer_guard가 잡으면 종결 |
| I-17 | ~~확률 질문 → forecast 페이지 안내~~ | s4 | ✅ 2026-09-01 — 확률 어휘 감지 시 결정론 문두 안내(_STOCK_UNSUPPORTED_NOTICES) |
| I-18 | 2종목 비교 고지 | s3 | ✅ 1-3에서 처리 |
| **I-19(신규)** | ~~**용어 결정론 풀이**~~ | q01·q06, q04는 무풀이 유지 | ✅ 2026-09-01 — `answer_guard.attach_glossary`(모멘텀·수급·%B·ATR·정/역배열, 질문에 있으면 생략·기존 괄호 존중·첫 등장만) |
| **I-20(신규)** | ~~**전문가 데이터 질문 라우팅**~~ | p04·p05·p09 | ✅ 2026-09-01 — 스코어 슬라이스가 계산하던 trend를 허브 `AreaScoreInfo.trend`로 개통, `_EXPERT_DETAIL_RE`(추이·산출근거·개폐업 등) 감지 시 `_trend_text`(최근 6분기)+산출 방식 블록 주입. 실DB 검증(목동문화체육센터 8분기) |
| **I-21(신규)** | **질문 유형별 서술 프레임 분리** — 진단(p03)·데이터(p04·p05·p09) 질문에는 추천 문형 금지 | p05·p09 | ⏳ 프롬프트 변경 — LLM 교체 후보 확정(Phase 5) 뒤 |

- **검증**: Phase 0-2 baseline 대비 골든셋 재실행(`risk_mention_rate` 1.0 유지·
  `dangling_citation` 0·신규 준수율 지표 추가) + 부록 A 30문 전량 재실행 비교.

---

## Phase 3 — 화면: 정보 과다·방향성 이해 (I-5·I-6·I-9)

2차 테스트 판정 "채팅은 과다하지 않으나 **화면이 과다**"의 처방. 기존 로드맵 항목 그대로.

1. ~~**I-6 detail 2단계 노출**~~ — ✅ 상권 완료(맥 2026-08-31, `be7e28d`) + **주식 완료
   (백엔드 PC 2026-09-01, 미커밋)**: 채팅 종목 카드 6필드 그리드에 쉬운 말 해석 한 줄
   (IndicatorPanel과 같은 임계값 — 무설명 해소. 카드가 `<button>`이라 접기 대신 해석 부착),
   페이지 형태 칩(불트랩 등)에 `title` 툴팁(클릭 전에도 설명 노출). 자료 패널
   (IndicatorPanel)은 이미 2단계(판정 지표 + "참고 지표" 접기)라 무변경.
2. ~~**I-5 매물대·지지/저항 POC 상단 배치**~~ — ✅ 완료(백엔드 PC 2026-09-01, 미커밋):
   AiInsightCard의 forecast 인사이트를 "근거 보기" 접기에서 꺼내 근거 그리드 바로 아래
   "가격대 근거"로 전면 배치.
3. ~~**I-9 "이 서비스가 못 하는 것" 명시**~~ — ✅ 완료(맥 2026-08-31, `be7e28d`): 상권 자료
   패널 말미 고지(임대료·권리금 미제공 / 추정 집계 / 분기 시차).
4. ~~점수 표기 원칙~~ — ✅ 완료(맥 2026-08-31, `be7e28d`): 컴포넌트 점수 방향 색(±5점 밴드) +
   기존 "50점=평균" 헤더 유지. **채팅 답변의 점수 서술은 1-1(백엔드 컨텍스트)의 몫**.
5. ~~**어닝 임박 배지**~~(SAVE 대조 편입) — ✅ 완료(맥 2026-08-31, `be7e28d`):
   `StockForecast.earnings_veto` 타입 + AiInsightCard 배지·설명. 정본은 [[ROADMAP]] §SAVE.
- **검증**: `pnpm run typecheck` + 페르소나 재실행(용어·이해도 축).

## Phase 4 — 시그널 대개편 잔여 ([[SIGNAL_OVERHAUL_2026-08]] 정본)

> ✅ **[6]+B9 알림 묶음 구현 완료(2026-09-01, 미커밋)** — 조건 슬라이스(recommendation
> `/price-alerts`)+허브 스캔 2종+n8n 워크플로+프로필 UI+중립 답변 안내 문장. 마이그레이션
> `n3a4b5c6d7e8`(배포 시 자동 적용). 잔여: n8n 임포트(사용자)·[5] KIS·[1]-②④.

5. **[5] KIS 어댑터** — 국내주식만 KIS, 미국은 yfinance 유지. `MarketDataPort` 구현체 추가.
6. **[6] 가격 도달 알림** — 새 슬라이스(조건 테이블+라우터 `/myself`+인터랙터+프론트 /profile 병합,
   발송은 bookmark_alert 패턴 재사용). **중립·약 답변의 공식 대체재** — q09(물린 초보)의 실제
   필요("내 손절선 알림")가 이것. 답변에서 알림 설정 안내 문장까지 연결(I-17과 같은 방식).
   **B9(티커 뉴스 알림 — ROADMAP §SAVE 대조, 2026-08-31)와 알림 인프라(조건·스캔·수신 설정)를
   공유하므로 한 묶음으로 설계한다.** B11(데일리 브리핑)은 B9 뒤 같은 파이프.
7. **[1]-②④** — 윈저라이징·급등 후 유지율(스키마 확장 필요).
8. 옛 정의 채점 스냅샷 104행 재채점(정의 혼재 정리 — 대개편 문서 기록).

## Phase 5 — 공개 전 필수 (행정·병렬, 개발과 독립)

9. **LLM 교체 판정(③-M5② / I-8 상향)** — EXAONE NC 라이선스는 공개 서비스 불가.
   후보 Llama 3.x/Gemma 3/Kanana pull → eval 120문항 회귀 대조 → `LLM_MODEL` 교체.
   ⚠ Phase 1·2의 프롬프트·가드 작업과 순서 주의: **가드는 결정론(모델 무관)이라 먼저 해도
   되지만, 프롬프트 튜닝은 교체 후보 확정 뒤에**(모델 바뀌면 재튜닝).
10. **유사투자자문업 신고([0-2], 사용자 행정)** — 완료 전 표현 해제([0-3]) 금지.
11. **보안 일괄** — Postgres·Neo4j 기본 비밀번호 교체(메모리 정본: 외부 공개 직전 한 묶음).
12. **오프사이트 백업 실행(②-M3 잔여)** — rclone gdrive 설정+첫 미러+주 1회 cron(절차는
    `backup_offsite.sh` 헤더), USB 1회 복사. PC 유실 시나리오가 현재 미방어.

## Phase 6 — 확장 (기존 권고 순서 유지)

13. I-4(뜨는 상권 필터 성별·연령·요일) → B7(공정위 가맹정보 — 선행 조건 없음) → B3(내 가게 진단)
    → B8(회수기간) → B11(데일리 브리핑 — B9 뒤) → lease 게이트 → B4(R-ONE 키 후) →
    B10(경제지표 캘린더 — 수요 게이트 뒤).
14. 상권 응답 속도 — 이번 실측 20~86초(주식 5~14초와 대비). 액세스 로그 `duration_ms`로
    p50/p95 측정 → phase1/phase2 프롬프트 토큰 다이어트·병렬화 검토. **측정 먼저, 최적화는 뒤**.

---

## 3차 페르소나 테스트 (2026-09-01, 멀티턴 20시나리오 × 2~3턴 = 55턴, 계정 +claudetest2)

1·2차와 달리 **대화를 이어가며**(같은 conversationId) 가드 지속성·맥락 승계를 쟀다.
전문은 대화 id 155~169·178~186(1차 실행 500 잔재 170~177은 무시).

**오늘 배포분 발화 확인(전부 ✅)**: 임대료·배당·확률 고지(멀티턴 유지), 등급 '주의' 고지,
쉼표 3종목 비교 고지, 길음1동 매칭, 랭킹 결정론 5곳, 지시어 승계(m01 "그 중에서"),
I-15 거리 교체("지지선 (현재가보다 76% 아래)"), 용어 풀이 괄호, 컴포넌트 방향 병기,
중립 답변 가격 알림 안내.

**발견(우선순위) — 4차 개선 백로그** (P1~P5 ✅ 2026-09-01 당일 수정·배포, P6·P7 ✅ 2026-09-03 `8a67725`, P8은 매물대만 ✅·뉴스 상세 잔여):
| # | 심각도 | 증상 | 처방 후보 |
|---|---|---|---|
| ~~P1~~ ✅ | 높음 | s09 "너네 신호 지난달에 몇 개 맞았는데?" → 메타 가드 미발화, general(Gemini) 낙하 → **"저는 OpenAI에서 개발한…" 자기 부정** 3턴 지속 | `_is_service_meta` 키워드 확장(너네·신호·맞았) + general 경로에 자기 정체성 시스템 프롬프트 |
| ~~P2~~ ✅ | 높음 | **비지시어 후속 질문의 맥락 붕괴** — "경쟁 몇 개야?"(m04)·"포화 아니야?"(m08)·"뭘 조심해야 해?"(m07)류에서 phase1이 전면 재선택 → 동대문·홍대·여의도로 리셋(6시나리오에서 재현). 업종도 표류(성수역→조명용품) | 직전 추천 승계 조건 확대: 직전 추천 존재 + **새 지역 미언급**이면 후보를 직전 추천으로 제한(지시어 불요). 업종도 동일 승계 |
| ~~P3~~ ✅ | 중간 | m03 t2 "그 중 첫 번째" → 총신대입구 — **랭킹 결정론 응답이 recommendations payload를 안 남겨** 지시어 가드가 이어받을 앵커가 없다 | 조건 라우팅 응답도 상위 N을 payload로 저장 |
| ~~P4~~ ✅ | 중간 | s10 t1 "PER 낮은 5개" → 리졸버 실패 **조기 반환 경로에 미지원 고지 누락**("종목을 찾지 못했습니다: PER 낮은…" 원문 노출) | `_answer_stock` 실패 경로에도 `_unsupported_notice` 삽입 |
| ~~P5~~ ✅ | 중간 | s06 "급등 종목 찍어줘" → market_news가 특정 종목 "급등·단기 투자 기회" 서술(사실상 찍기), 후속에서 "마이크론 테크놀로지" 리졸버 실패 반복 | 급등주 질의 결정론 거절 문구 + 마이크론 등 별칭 보강 |
| ~~P6~~ ✅ | 낮음 | "떨어지면 알려줘"(s02·s03 t3) 명시 요청에 가격 알림 안내 미보장(중립일 때만 부착) | ✅ 수정(9/3) — `_ALERT_HOWTO_RE`에 조건 어미(-면)+알려 변형 추가. 조건 어미를 요구해 "떨어지는 이유 알려줘" 설명 요청과 구분 |
| ~~P7~~ ✅ | 낮음 | m10 t2 "강남 말고" → 논현역(강남구) — 부정 조건 미반영 | ✅ 수정(9/3) — P4-8과 동일 처방(`_excluded_area_codes`) |
| P8 | 낮음 | s05 t2 매물대 POC "구간 어디야?"에 수치 미서술, s08 t2 "그 뉴스가 뭔데?"에 지표 분석으로 응답 | 매물대는 ✅(9/3, P4-11과 동일 처방). **뉴스 상세 후속 라우팅은 잔여** |

**인프라 인시던트**: 테스트 연속 부하 중 커널 OOM이 llama-server(anon 7.15GiB)를 재차 kill
— 8/28과 동일 유형(이번엔 가중치가 익명 메모리 상주 상태). 연쇄 증상: 상권 응답 200초+
타임아웃 → 즉시 500 "AI 응답 파싱 실패" 다발(500이어도 대화·사용자 메시지는 생성됨 —
대화 목록에 잔재). `.wslconfig` 메모리 상향 권고 재확인.

## 신호 보드 "하락 0개" (2026-09-01 사용자 발견 — 배포·데이터 정합 함정)

- 원인: 3d8ecbb가 하락 임계를 코드 기본값으로만 -0.45로 바꿨고, **이미 저장된 활성
  config 행(refit-20260822)은 -1.01(도달 불가)로 남았다.** 8/29 주간 재적합도 구 코드로
  돌아 -1.01을 복제. 스냅샷은 활성 행을 읽으므로 DOWN이 구조적으로 0.
- 조치: 재적합을 새 코드로 즉시 재실행(`POST /automation/forecast-refit`, promote) →
  `refit-20260901` 승격(RSI+BB 0.5/0.5 @0.35, **down -0.45**, n=125 · 하한 0.496 >
  기준선 0.340). DB 직접 UPDATE는 분류기 차단 — 제품 경로(재적합)로 해소한 것이 결과적으로 정석.
- 오늘 캡처분(as_of 8/31, 77행)은 insert가 do-nothing이라 구 임계 그대로 — **내일 14:00
  스냅샷부터 하락 신호가 화면에 나타난다.**
- 교훈: **코드 상수를 바꾸는 커밋은 그 값을 이미 저장한 활성 행(설정 테이블)의 이관까지
  포함해야 한다** — 마이그레이션 또는 배포 직후 재적합 1회를 절차에 명시.

## 부록 A — 재현 시나리오 30문 (골든셋 편입·재검증용)

1차(기능): ①홍대 카페 창업 ②강남역 치킨vs커피 ③성수동 운영자 진단 ④유동인구多·폐업률低 3곳(422)
⑤망원동 임대료(회피) ⑥삼성전자 사도 돼 ⑦하이닉스 왜 떨어져 ⑧테슬라vs애플(422급 오류)
⑨엔비디아 확률 ⑩인텔 뉴스.

2차(페르소나): p01 퇴직금 치킨집 서울 어디(422) · p02 신촌 대학생 장사 · p03 연남동 카페 3년차
손님 감소 진단 · p04 성수동 분기 매출 추이 데이터(★왜곡) · p05 잠실 개폐업 추이 · p06 목동
반찬가게(422) · p07 관악 배달 분식 · p08 서울대입구 편의점 · p09 홍대 종합점수 산출근거 ·
p10 겁많은 초보 강북 · q01 주식 처음 삼성전자 · q02 카카오 물타기 · q03 은퇴 배당주 ·
q04 삼성전자 RSI/%B/OBV · q05 너희 백테스트 적중률(★메타) · q06 나스닥 모르는데 애플 ·
q07 관세 반도체 매도 · q08 급등주 · q09 엔비디아 물림 · q10 하이닉스 매물대.

## 부록 B — 이번 테스트의 종합 판정 (요약)

- 인증·이력 구간 무결(가입 1.0s·로그인 1.2s·대화 복원 정상).
- 30건 중 유용 11 · 애매 12 · 실패 7(422 4건 + 왜곡 2건 + 메타 1건).
- 상권 답변은 전문용어 0건(쉬움)이나 얕고, 주식 답변은 깊으나 초보에게 어렵다.
- 카드 배지(중립·약)와 본문 서술의 모순 5건 — answer_guard 배포 후 재측정.
- 결론: 정보의 몸통은 경쟁 우위가 있으나 전달이 페르소나 무차별. Phase 1~3이 그 처방이다.

## 4차 페르소나 테스트 (2026-09-02, 멀티턴 20시나리오 × 5턴 = 100턴, 계정 +claudetest4)

업종 결정론 가드(33830a3) 배포 직후 실측. 러너: 세션 스크래치패드 persona4/(결과 사본
`~/.claude/projects/-home-host-projects-com-redoceanmap/persona4/results-20260902.jsonl`).
턴 100 · 에러 1(500) · 응답 중앙값 10.8s · 최대 64.6s(첫 콜드 스타트).

### 통과 (배포 가드 실서비스 발화 확인)

- **업종 결정론 가드 전승**: 떡볶이→분식(M1, 어제 실버그 5턴 재현 — 정정·반박에도 유지),
  국밥→한식·중도 돈까스→일식(M4), 빵집·케이크→제과점(M9). 별칭 사전 전 케이스 적중.
- 등급 '주의' 고지(M5~M9 전반) · 임대료/배당 무지원 고지(M2·S6) · 조건 질의 랭킹(M5) ·
  적중률 메타 결정론 응답(S4 t5, 0.0s) · 중립 시 가격 알림 안내(S2 t3 — 프로필 경로 정확) ·
  정체성 프리앰블(자기 부정 0건) · 매수/매도 단정 회피 일관 · 멀티 티커 전환(S3).

### 발견 결함 (P4-*)

| # | 증상 (시나리오) | 처방 방향 | 심각도 |
| --- | --- | --- | --- |
| P4-1 | phase2 areas 항목 `reason` 누락 시 KeyError→**500** (M8 t1) | ✅ 수정(9/2, `8a1789b`) — `item.get("reason","")` 열화 | ★높음 |
| P4-2 | general(Gemini)이 **기능·UI를 발명** — "종 모양 아이콘"(S1 t5), "상권 권리금/임대료 변동 알림"(S9 t5) | ✅ 수정(9/2, `8a1789b`) — 프리앰블 실기능 화이트리스트 + 알림 설정법 결정론 응답(`_ALERT_HOWTO_RE`) | ★높음 |
| P4-3 | 지표 해석 모순 — RSI 40.3을 "과매수"로, 직전 턴과 과매도↔과매수 반전, 감성 +0.30을 "악재 다수"로(S2 t3·S9 t4) | ✅ 수정(9/2, `8a1789b`) — `enforce_overheat_claim`/`enforce_sentiment_claim`. 채점기 대응 규칙 `overheat_mismatch`·`sentiment_mismatch`는 9/3 `8a67725`(가드 함수를 그대로 호출해 정의처 단일화) | ★높음 |
| P4-4 | "국밥 **말고** 돈까스"(업종 제외)가 지역 승계까지 해제 → 노원→이태원(M4 t4) | ✅ 수정(9/2) — `_exclusion_targets_service`: 말고/빼고 앞 어절이 업종 단서면 지역 승계 유지 | 중간 |
| P4-5 | '네이버' 종목 미해석 — 시나리오 전멸(S5) | ✅ 수정(9/2, `8a1789b`) — 네이버→NAVER 별칭 | 중간 |
| P4-6 | 급등주 거절 가드가 market_news 경로 미적용 — "내일 급등할 종목"에 뉴스 서술(S4 t1) | ✅ 수정(9/2, `8a1789b`) — 급등주 거절 정규식 관형형 어미 커버 | 중간 |
| P4-7 | 후속 단문 general 이탈(M1 t5·M3 t4) — 3차 P8 재확인 | ✅ 수정(9/3, `8a67725`) — `_inherit_intent`: general 판정에 지시어 또는 도메인 어휘가 있고 직전 카드(추천/랭킹/종목)가 있으면 그 도메인으로 승계. 히스토리는 phase0 프롬프트에 이미 있으나 7.8B가 안 써서 코드가 승계 | 중간 |
| P4-8 | "홍대 말고 다른 데"에 홍대 계열 추천(M2 t4) — 3차 P7 재확인 | ✅ 수정(9/3, `8a67725`) — `_excluded_area_codes`: 말고/빼고/제외 앞 어절의 지역을 언급·후보 집합에서 제거, 공집합이면 매출 상위 비제외 상권으로 대체 | 중간 |
| P4-9 | 설명형 질문("점수 어떻게 계산해?"·"상권 분석이 뭔데?")에 추천 발사(M7 t1·t2, M10 t2) | ✅ 수정(9/2) — `_METHOD_QUERY_RE` 지역 미언급 시 방법론 설명 결정론 응답 | 낮음 |
| P4-10 | "방학엔 장사 안 되지 않아?"의 방학→도봉구 방학역 오인(M8 t2, 500 여파 복합) | ✅ 수정(9/2) — `_HOMONYM_STEMS`(방학·신사·대치): 장소 접미 동반 시만 지명 인정 | 낮음 |
| P4-11 | "아까 현대차 매물대가 어디랬지?"에 매물대 무응답·종목 불명(S9 t4) | ✅ 수정(9/3, `8a67725`) — 질문에 '매물대'가 있는데 본문에 없으면 코드가 POC 구간 문장을 붙이고, 표본 부족이면 산출 불가 고지 | 낮음 |

- 관찰: S3 t5 "셋 중 하나만"에 마지막 종목만 서술(비교 미구성), S8 t4 "상승 신호 종목"을
  실제 신호 데이터가 아니라 뉴스로 대답(신호 조회 라우팅 부재), S8 t1 메타 질문 첫 턴은
  general로 가나 "수치를 지어내지 않는다"로 안전.

### 4차 마감 (2026-09-03, `8a67725` 배포·/health 확인)

P4-1~11 전부 ✅(P4-1·2·3·5·6 → `8a1789b`, P4-4·9·10 → `e0560c2`, P4-7·8·11 → `8a67725`).
골든셋 사이클을 함께 완주했다:

- 골든셋 129→**134**: MR27(길음동 떡볶이)·MR28(관악구 방학 — 동음이의 회귀)·MT11(국밥 말고
  돈까스 — 업종 제외 승계)·MT12(떡볶이 정정 — 업종 결정론)·MN12(점수 방법론). SU04·SU08
  accepted_queries에 영문 사명(Microsoft·Netflix) 허용.
- 채점기 신규 2규칙 `overheat_mismatch`·`sentiment_mismatch`(P4-3 가드의 채점 축 편입).
- 러너 3회 재완주 중 발견·수정: 반경 미적용 고지의 "추천" 어휘가 등급 가드 치환 뒤
  `grade_caution`에 걸림(MR21 → "결과"로), 공백 결합+티커 혼재 다종목(SF06 "테슬라 AAPL
  애플" → 대문자 티커 토큰 동반 시만 분해), 가드 삽입 문장의 마커 누락·과열 교정 표현의
  수치가 citation 커버리지를 깎음(삽입문 `[1]` 부착·교정 표현에서 수치 제거).
- **baseline 재박제(134)**: intent 1.0 · stock_query 1.0 · region_hit 1.0 · inherit 1.0/1.0 ·
  volume_verdict 1.0 · risk_mention 1.0 · **citation_coverage 0.911**(구 0.6) ·
  phase1_guard_activation 0.638 · 환각 **1**(구 5, MR15 37000) · 잘림 0 · 절대 위반 0.
  구 129본은 git 이력(`ce9fd3d`).
- 잔여(다음 사이클): 3차 P8의 뉴스 상세 후속("그 뉴스가 뭔데?") 라우팅, 4차 관찰 2건(다종목
  비교 미구성·"상승 신호 종목" 신호 조회 라우팅 부재), I-11 표 확장, 보드 up_rate 방향 라벨.
