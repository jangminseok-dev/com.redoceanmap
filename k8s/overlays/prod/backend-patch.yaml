apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  template:
    spec:
      containers:
        - name: backend
          env:
            - name: ENV
              value: production
            - name: LOG_FORMAT      # 구조화 로깅(③-M4) — kubectl logs에서 필드 검색
              value: json
